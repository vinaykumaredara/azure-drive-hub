[build]
  command = "bash -lc \"rm -rf node_modules package-lock.json && npm install && npm run build\""
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.19.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
