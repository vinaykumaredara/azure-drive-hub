// src/hooks/use-auth.ts
// Shim to satisfy legacy imports — re-export the real useAuth hook.
// Adjust the RHS path to where your real hook lives.

export { useAuth } from '@/components/AuthProvider';